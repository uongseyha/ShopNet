# version: '3.8'

services:
  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: mssql-server
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=4@Test321
      - MSSQL_PID=Developer
    ports:
      - "1433:1433"
    # volumes:
    #   - mssql-data:/var/opt/mssql
    # networks:
    #   - app-network
    # restart: unless-stopped

#   api:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: dotnet-api
#     environment:
#       - ASPNETCORE_ENVIRONMENT=Development
#       - ASPNETCORE_URLS=http://+:80
#       - ConnectionStrings__DefaultConnection=Server=mssql;Database=StoreDB;User Id=sa;Password=YourStrong@Password123;TrustServerCertificate=True
#     ports:
#       - "5000:80"
#     depends_on:
#       - mssql
#     networks:
#       - app-network
#     restart: unless-stopped

# volumes:
#   mssql-data:

# networks:
#   app-network:
#     driver: bridge